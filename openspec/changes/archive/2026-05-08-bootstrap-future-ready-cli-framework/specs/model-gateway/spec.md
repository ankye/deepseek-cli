## ADDED Requirements

### Requirement: Provider-Neutral Model Gateway

The system SHALL define a model gateway abstraction that can support DeepSeek as the default provider and additional providers without changing runtime code.

#### Scenario: Runtime calls model through gateway

- **WHEN** the runtime needs a model response
- **THEN** it calls the configured model gateway rather than a provider-specific client directly

### Requirement: Streaming Model Events

The model gateway SHALL expose streaming output in a provider-neutral format that can carry text deltas, tool calls, usage, finish reasons, and provider errors.

#### Scenario: Stream text delta

- **WHEN** the provider returns partial assistant text
- **THEN** the model gateway emits a provider-neutral text delta event

#### Scenario: Stream tool call

- **WHEN** the provider returns a tool call
- **THEN** the model gateway emits a provider-neutral tool call representation

### Requirement: Tool Schema Compatibility

The model gateway SHALL accept tool schemas generated by the capability registry and translate them into the selected provider's request format.

#### Scenario: Send capability schemas to model

- **WHEN** the runtime prepares a model request with registered tool capabilities
- **THEN** the gateway includes compatible tool definitions for the selected provider

### Requirement: Deterministic Mock Adapter

The system SHALL provide a mock model adapter for tests and early framework validation.

#### Scenario: Test runtime without network

- **WHEN** tests use the mock model adapter
- **THEN** runtime tests complete without external network access or provider credentials
